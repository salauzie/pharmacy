%h1 Patient Search

= form_tag patients_path, method: :get, role: "search" do
  = text_field_tag 'q', params[:q], id: "srch-term", placeholder: "Search Patients" 
  = submit_tag "Search"
%br
- @patients.each do |f|
  = link_to f.first_name + ' ' + f.last_name, patient_path(f)
  %br 